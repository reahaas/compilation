/* print fibonacci sequence */
/* Only numbers greater than 200 */
/* print the numbers twice. */
a, b, c: int;
{
    a = 0;
    b = 1;

    while (a < 1000) {

       c = a + b;
       a = b;
       b = c;
       if (a < 200)
           break;
       else
           output(a);
       output(a);
    }
	output(a);
}
