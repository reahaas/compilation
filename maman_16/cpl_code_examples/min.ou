/* Min - Finding minimum between two numbers */
a, b: float;
c: int;
{
   input (a);
   input (b);

   c = static_cast<int> (b);

   if (a < c)
     output(a);
   else
     output(c);
}
