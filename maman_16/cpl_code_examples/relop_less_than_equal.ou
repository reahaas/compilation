/* if a<=b: print "1" */
/* else: print "0" */
a, b: float;
c: int;
{
   input (a);
   input (b);

   c = static_cast<int> (b);

   if (a <= b)
     output(1);
   else
     output(0);
}
